#Python simple calculator

It does basic calculations which innvolves subtraction and addition;it does this by asking the user to substitute the value of a and b.

What I learnt:

-user input

-variables

-conditional statements(if,elif,else)
